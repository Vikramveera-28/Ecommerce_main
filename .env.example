# Local frontend development can proxy /api to the Flask backend through Vite.
# Leave VITE_API_BASE_URL empty in Vercel so the app uses same-origin /api/v1.
VITE_API_BASE_URL=

# Vercel dashboard variables:
MONGODB_URI=
MONGODB_DB=ecommerce
AUTH_SECRET=
